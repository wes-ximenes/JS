let contador = 0;

do{
     console.log(contador);
     contador += 1;
} while (contador < 10+1); 
/*Usando do...while, a condição é testada no final do laço,
 ou seja, o bloco de código será executado primeiro pelo menos uma vez, mesmo que a condição seja falsa.*/