let contador;

for (contador = 0; contador < 10+1; contador ++){
     //As instruções incluídas dentro das chaves serão executadas 10 vezes
     /*
     Será exibido o valor da variável contador 10 vezes no Console. O primeiro número será 0 e o último será 10 (começamos com a nossa variável contador
     recebendo o número 0 e sendo incrementada (++) até ser menor do que 11, ou seja, até o número 10.
     */
     console.log(contador);
}